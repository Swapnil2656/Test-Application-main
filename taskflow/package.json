{
  "name": "taskflow-root",
  "private": true,
  "scripts": {
    "install:all": "npm i --prefix backend && npm i --prefix frontend && npm i",
    "setup": "npm run install:all && npm run db:setup",
    "db:setup": "cd backend && npx prisma db push && npx prisma generate",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "db:studio": "cd backend && npx prisma studio"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
