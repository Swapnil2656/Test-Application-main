[providers]
node = {}

[phases.install]
cmds = ["cd taskflow/backend && npm install"]

[phases.build]
cmds = ["cd taskflow/backend && npm run build"]

[start]
cmd = "cd taskflow/backend && npx prisma db push --accept-data-loss && npm start"
